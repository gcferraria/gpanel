(function( JsB ) {
    
    var
        DateRange = my.Class( JsB, {
            'constructor': function( elem, caller ) {
                DateRange.Super.call( this, elem, caller );
            }
        })
    ; 
    
    JsB.object( 'App.DateRange', DateRange );

})( JsB );
